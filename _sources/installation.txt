.. highlight:: shell

============
Installation
============

At the command line::

    $ pip install syncer

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv syncer
    $ pip install syncer
